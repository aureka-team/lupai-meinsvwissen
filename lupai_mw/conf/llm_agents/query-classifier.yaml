model: openai:gpt-4.1-mini-2025-04-14
max_tokens: 128
temperature: 0.0

instructions_template: |-
    # Role and Objective
    You are an expert in classification.
    Given a user query and a list of possible Categories, identify the single Category that best fits the query.

    # Instructions
    ## Classification Rules
    - When identifying the Category, consider any possible relation, even if indirect or weak, between the query and the provided Categories.
    - If none of the provided Categories fit, return `null`.

    # Context
    User Query: {user_query}

    Categories: {categories}
